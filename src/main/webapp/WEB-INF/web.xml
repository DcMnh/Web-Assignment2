<?xml version="1.0" encoding="UTF-8"?>
<web-app version="6.0" xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd">

    <!-- JSF Development Mode -->
    <context-param>
        <param-name>jakarta.faces.PROJECT_STAGE</param-name>
        <param-value>Development</param-value>
    </context-param>

    <!-- Faces Servlet Configuration -->
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>/faces/*</url-pattern>
    </servlet-mapping>

    <!-- Session Timeout -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- Welcome Page -->
    <welcome-file-list>
        <welcome-file>faces/index.xhtml</welcome-file>
    </welcome-file-list>

    <!-- ========================== -->
    <!-- SECURITY CONFIGURATIONS -->
    <!-- ========================== -->

    <!-- RESTful API Security -->
    <security-constraint>
        <display-name>RESTful API Security</display-name>
        <web-resource-collection>
            <web-resource-name>RESTful Resources</web-resource-name>
            <url-pattern>/Slider/resources/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>RESTFullGroup</role-name>
            <role-name>Admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- JSF Slider Pages -->
    <security-constraint>
        <display-name>JSF Slider Pages Security</display-name>
        <web-resource-collection>
            <web-resource-name>JSF Pages</web-resource-name>
            <url-pattern>/faces/slider/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>WEBGroup</role-name>
            <role-name>Admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- JSF Admin Pages -->
    <security-constraint>
        <display-name>Admin Area Security</display-name>
        <web-resource-collection>
            <web-resource-name>Admin Pages</web-resource-name>
            <url-pattern>/faces/appUser/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>Admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Public access to index.xhtml -->
    <!-- REMOVE THIS to protect index.xhtml -->
<security-constraint>
    <web-resource-collection>
        <web-resource-name>Index Access</web-resource-name>
        <url-pattern>/faces/index.xhtml</url-pattern>
    </web-resource-collection>
    <auth-constraint>
        <role-name>RESTFullGroup</role-name>
        <role-name>WEBGroup</role-name>
        <role-name>Admin</role-name>
    </auth-constraint>
</security-constraint>


    <!-- Basic Authentication -->
    <login-config>
        <auth-method>BASIC</auth-method>
        <realm-name>ApplicationRealm</realm-name>
    </login-config>

    <!-- Declare roles -->
    <security-role>
        <role-name>RESTFullGroup</role-name>
    </security-role>
    <security-role>
        <role-name>WEBGroup</role-name>
    </security-role>
    <security-role>
        <role-name>Admin</role-name>
    </security-role>

</web-app>
